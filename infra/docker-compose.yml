services:
  mongo:
    image: mongo:7
    container_name: erxes-ai-assistant-gateway-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
